#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdlib.h>
#include <stdio.h>

int _putchar(char c);
void _printf(const char *format, ...);
void _puts(char *s);
int _sterlen(char *n);
void print_integer(int n);
void print_float(double num);
*int_to_binary(int num);

#endif
